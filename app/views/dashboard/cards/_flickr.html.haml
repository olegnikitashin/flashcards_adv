%br
%button#flickr.btn.btn-default Загрузить из Flickr:
%br
%br
#flickr-field.hidden-field
  Найти во Flickr:
  %form#new_link.form-group
    = form_tag find_flickr_path, method: :put, remote: true do
      = text_field_tag :tag
      = hidden_field_tag :authenticity_token, form_authenticity_token
      = submit_tag 'Search', class: 'btn btn-default', id: 'flickr-search'
  #photos
    - if @urls
      - @urls.each do |url|
        = image_tag(url)
