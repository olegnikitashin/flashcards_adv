%button#flickr.btn.btn-default Загрузить из Flickr:
#flickr-field
  Найти во Flickr:
  %form#new_link
    = simple_form_for card, url: :find_flickr, remote: true do |f|
      = hidden_field_tag :tag
      = hidden_field_tag :authenticity_token, form_authenticity_token
      = f.button :submit, 'Search', class: 'btn btn-default', id: 'flickr-search'
    #photos
      - if @urls
        - @urls.each do |url|
          = image_tag(url)
